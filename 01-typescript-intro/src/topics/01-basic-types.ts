const name : string = 'strider'; 
let hpPoints : number|string = 95;// asignamos con el pipe operator para que permita el dato que yo quiera
const isAlive : boolean = true;

console.log({name, hpPoints, isAlive});

export {};